import React from 'react';

export default function UXPinWrapper ({children}) {
    return (
        <React.Fragment>
        {children}
        </React.Fragment>
    );
                
            

}